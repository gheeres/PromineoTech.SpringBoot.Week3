spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/myworld
    username: root
    password: password
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    disable-swagger-default-url: true